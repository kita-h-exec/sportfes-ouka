# === Directus (optional) ===
# Public base URL of your Directus instance (e.g. https://example.directus.app)
NEXT_PUBLIC_DIRECTUS_URL=
# Static token for reading protected collections (optional)
DIRECTUS_STATIC_TOKEN=
# Allow falling back to public client when token is invalid ("1" to enable)
DIRECTUS_ALLOW_TOKEN_FALLBACK=
# Debug logs for Directus helper ("1" to enable)
DEBUG_DIRECTUS=

# === Web Push (PWA notifications) ===
# Contact email or URL shown in VAPID claims (e.g. mailto:you@example.com)
WEB_PUSH_CONTACT=mailto:you@example.com
# VAPID key pair (generate via scripts/generate-vapid-keys.mjs or web-push CLI)
WEB_PUSH_PUBLIC_KEY=
WEB_PUSH_PRIVATE_KEY=

# Store for push subscriptions: memory | file | directus
PUSH_STORE=file
# Path used when PUSH_STORE=file
PUSH_FILE_PATH=data/push_subscriptions.json
# Debug logs for push store ("1" to enable)
DEBUG_PUSH_STORE_LOG=

# Secret header value required by /api/push/send (x-notify-secret)
NOTIFY_SECRET=change-me-long-random

# === AI (optional, for /ai API) ===
GOOGLE_API_KEY=
GEMINI_MODEL_NAME=gemini-2.5-flash
